build/
.work/
*.c
!runtime/*.c
.DS_Store

# stray executables `keal build` leaves in the working directory
/repro
/t[0-9]*

# a signing secret an example made; never in git
*.secret
*.db

# what the file-serving test generates
build/served/
build/uploaded.bin
build/kept-*.bin
